; EXAMPLE ONLY — do not use this file directly.
; Godot writes the real presets to export_presets.cfg (which is gitignored).
; Create presets in the Godot Editor, then copy values from this checklist.
;
; Required presets for store submission:
; - Android (AAB, Release, signed)
; - iOS (Release, signed via Xcode)
;
; Canonical identifiers:
; - Package/Bundle: com.rcbiscuits.angryanimals
; - IAP product: full_game_unlock
;
; iOS
; - Bundle Identifier: com.rcbiscuits.angryanimals
; - Display Name: Angry Animals
; - Version: 1.0
; - Build Number: 1
; - Minimum iOS: 14.0
; - Orientation: Portrait
; - Signing: Apple Distribution cert + App Store provisioning
; - Info.plist usage strings:
;   - NSCameraUsageDescription
;   - NSPhotoLibraryUsageDescription
;   - (Optional) NSUserTrackingUsageDescription (if using personalized ads)
;
; Android
; - Package Name: com.rcbiscuits.angryanimals
; - Version Name: 1.0
; - Version Code: 1
; - Min SDK: 28
; - Target SDK: 34
; - Architectures: ARM64 (required) + (optional) ARMv7
; - Export format: Android App Bundle (.aab)
; - Signing: upload keystore (keystore path, alias, passwords)
